[package]
name = "py-secure-enclave"
version = "1.0.0"
edition = "2024"

[lib]
name = "py_secure_enclave"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.28", features = ["extension-module"] }
secure-enclave-rs = { path = "..", features = ["jwt"] }
serde_json = "1.0"
